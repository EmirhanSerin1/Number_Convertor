import 'package:flutter/material.dart';

//We can change the Theme color in here

const light = Color(0xFF000000);
const ligthBackground = Color(0xFFFFFFFF);
final lightTheme = ThemeData(
  primaryColor: light,
  backgroundColor: ligthBackground,
);

const dark = Color(0xFFFFFFFF);
const darkBackground = Color(0xFF3F3C3C);
final darkTheme = ThemeData(
  primaryColor: dark,
  backgroundColor: darkBackground,
);
